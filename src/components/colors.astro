---
export interface Props {
    colors: Array<String>;
}

const { colors } = Astro.props;
---

<ul>
    {
        colors.map((item) => (
            <li
                class="mr-2 inline-block h-7 w-7 rounded-full border-2 border-accent"
                style={{ 'background-color': item.toString() }}
            />
        ))
    }
</ul>
